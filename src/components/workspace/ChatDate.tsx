import styled from "styled-components";

function ChatDate({chatDate}:{chatDate:string}) {
  return (
    <StContainer>
        {/* {chatDate.} */}
    </StContainer>
  )
}

export default ChatDate;

const StContainer = styled.div`
    
`;